apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "unifi-video.fullname" . }}-test-connection"
  labels:
{{ include "unifi-video.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "unifi-video.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
